class Lock {
  constructor(textContent, pwd) {
    this.crypto = crypto;
    this.txt = textContent.slice(0, myString.length / 2);
    this.enc = textContent.slice(myString.length / 2, myString.length);
  }

  hash() {
    
  }
}
